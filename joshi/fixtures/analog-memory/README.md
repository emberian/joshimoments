# Earlier-only analog memory fixtures

The schedule is outcome-blind. Every query/candidate pair is checked at the candidate's own
decision cutoff, with strict earlier ordering, explicit missing/gap policy, and deterministic ID
tie-breaking. Retrospective outcome joins are separate interventions and never alter decision-mode
artifacts.
