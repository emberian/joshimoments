"""Pre-registered conditional-settlement study (P(settle Up | path so far, time left)).

Protocol: REGISTRATION.md here, written before the fine series was fetched. Governing
principle: a defense against counting the wrong thing — ground-truth gate first, then
resolution, then counting, then calibration as self-refutation.
"""
