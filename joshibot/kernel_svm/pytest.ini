# Standalone from the repo's pyproject: that one sets testpaths=["tests"], which would make
# `pytest` run from here collect nothing and report green. An empty green run is the exact
# shape of failure this directory exists to prevent, so the config is pinned locally.
[pytest]
testpaths = .
python_files = test_*.py
addopts = -q
